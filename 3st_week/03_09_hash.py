# hash table 이란
# 키와 값으로 되어있는 자료 구조
# 파이썬의 딕셔너리와 동일하다.
# 파이썬의 딕셔너리는 hash table이라는 방식으로 통해서 구현한 것이다.

dict = {"fast": "빠른", "slow": "느린"}
# 키에 대한 검색이 가능
# 딕셔너리가 내부적으로 배열을 사용한다.

# fast와 slow에 해당하는 값들이 배열 어디에 위치하는지 알기 위해서 사용하는 함수가 바로 hash함수
